spring.application.name=task-manager

# PostgreSQL Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-round-art-apmbfzfd.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_GlS0vqNW8hfj}
spring.datasource.driver-class-name=org.postgresql.Driver

# H2 Database Configuration for Local Testing
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.h2.console.enabled=true

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Static Resources
spring.web.resources.static-locations=classpath:/static/

# Server Port
server.port=${PORT:8081}
