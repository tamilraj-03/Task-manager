spring.application.name=task-manager

# PostgreSQL Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d7fptvq8qa3s738blmh0-a.oregon-postgres.render.com:5432/task_manager_lz16?sslmode=require}
spring.datasource.username=${DB_USERNAME:task_manager_lz16_user}
spring.datasource.password=${DB_PASSWORD:EAFR0vH7rbWCNJ6kvVpIBCLgByT1yPKy}
spring.datasource.driver-class-name=org.postgresql.Driver

# H2 Database Configuration for Local Testing
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.h2.console.enabled=true

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Static Resources
spring.web.resources.static-locations=classpath:/static/

# Server Port
server.port=${PORT:8081}
